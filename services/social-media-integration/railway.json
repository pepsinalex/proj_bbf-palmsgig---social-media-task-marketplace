{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "../../Dockerfile"
  },
  "deploy": {
    "startCommand": "poetry run uvicorn src.social_media_integration.main:app --host 0.0.0.0 --port $PORT"
  }
}