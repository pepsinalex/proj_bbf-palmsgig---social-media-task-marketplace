{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "../../Dockerfile"
  },
  "deploy": {
    "startCommand": "poetry run uvicorn src.payment_wallet.main:app --host 0.0.0.0 --port $PORT"
  }
}