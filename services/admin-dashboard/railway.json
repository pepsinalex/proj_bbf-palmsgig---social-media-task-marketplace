{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "../../Dockerfile"
  },
  "deploy": {
    "startCommand": "poetry run uvicorn src.admin_dashboard.main:app --host 0.0.0.0 --port $PORT"
  }
}