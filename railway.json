{
  "build": {
    "installCommand": "poetry install --only=main",
    "buildCommand": "poetry build"
  },
  "deploy": {
    "startCommand": "python -m uvicorn src.api_gateway.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health"
  }
}